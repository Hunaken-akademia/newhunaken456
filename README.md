# 締切前スナップショット サーバー保存 v134

上書きするファイル：
- `api/yoso.js`
- `pipeline/capture_all_active_races.mjs`

変更内容：
- GitHub Actionsの30分取得で、締切前の6艇分データが揃ったレースだけSupabaseへ保存
- 締切前は取得のたびに最新スナップショットへ更新
- 締切後は凍結済みの締切前スナップショットを保持し、確定後データで上書きしない
- `final=0`が誤って締切後に呼ばれても、サーバー側で締切判定して上書きを防止
- Actionsログへ `preRace=updated / kept / missing` を表示
- 実行末尾に保存対象数・保存成功数・未保存レースを表示

この段階ではAI買い目の生成・保存方法は変更していません。
次の段階で、一括記録をこの凍結スナップショット参照へ切り替えます。
